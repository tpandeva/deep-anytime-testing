# @package _global_
defaults:
  - /data: cifar10
  - /model: mlp
  - /operator@tau1:
      - net

project: "Cifar10-AA-MMD"


tau1:
  net:
    file_to_model: "/var/scratch/tpandeva/deep-anytime-testing/data/cifar10/best_model.pth"

tau2: null

data:
  data_seed: 0
  samples: 64
  file: "/var/scratch/tpandeva/deep-anytime-testing/data/cifar10/cifar10/"
  file_to_model: "/var/scratch/tpandeva/deep-anytime-testing/data/cifar10/best_model.pth"

train:
  seqs: 60
  name: "mmd"
  seed: 0
  bs: 64